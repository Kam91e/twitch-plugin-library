---
title: "Streamer.bot — Sub-Actions: Integrations > Tits"
category: "automation"
type: "Reference"
tags:
  - streamer-bot
  - sub-actions
  - integrations
source: "https://docs.streamer.bot/api/sub-actions/integrations/tits"
last-reviewed: 2026-07-31
---

# Integrations — Tits

> Sub-actions to activate triggers and throw items in T.I.T.s.

## Activate Trigger by Name

**Location:** `Integrations > Tits > Activate Trigger by Name`
**Add behavior:** dialog — opens a config dialog on selection (evidence: `subaction-Integrations-Throwing-System-Activate-Trigger-by-Name-dialog-uia.txt`; see `creation-flows-catalog.txt`)
**Description:** Allows for activating T.I.T.s trigger by name
**Parameters:** App-verified from `subaction-Integrations-Throwing-System-Activate-Trigger-by-Name-dialog-uia.txt`, `subaction-Integrations-Throwing-System-Activate-Trigger-by-Name-dialog.png` (Add SubAction - Integrations > Throwing System > Activate Trigger by Name, ~600×139): `Trigger` (text), `Test` (button), `Not Connected` (button); Ok/Cancel present.
**Variables:** Not documented in official docs

## Activate Trigger

**Location:** `Integrations > Tits > Activate Trigger`
**Add behavior:** dialog — opens a config dialog on selection (evidence: `subaction-Integrations-Throwing-System-Activate-Trigger-dialog-uia.txt`; see `creation-flows-catalog.txt`)
**Description:** WIP
**Parameters:** App-verified from `subaction-Integrations-Throwing-System-Activate-Trigger-dialog-uia.txt`, `subaction-Integrations-Throwing-System-Activate-Trigger-dialog.png` (Add SubAction - Integrations > Throwing System > Activate Trigger, ~600×139): `Trigger` (button), `Not Connected!` (button), `Test` (button), `Not Connected` (button); Ok/Cancel present.
**Variables:** Not documented in official docs

## Throw Item by Name

**Location:** `Integrations > Tits > Throw Item by Name`
**Add behavior:** dialog — opens a config dialog on selection (evidence: `subaction-Integrations-Throwing-System-Throw-Item-by-Name-dialog-uia.txt`; see `creation-flows-catalog.txt`)
**Description:** Allows for throwing a specific item by name
**Parameters:** App-verified from `subaction-Integrations-Throwing-System-Throw-Item-by-Name-dialog-uia.txt`, `subaction-Integrations-Throwing-System-Throw-Item-by-Name-dialog.png` (Add SubAction - Integrations > Throwing System > Throw Item by Name, ~600×221): `Item` (text), `Delay` (text), `seconds` (text), `Amount` (text), `Test` (button), `Not Connected` (button); Ok/Cancel present.
**Variables:** Not documented in official docs

## Throw Item

**Location:** `Integrations > Tits > Throw Item`
**Add behavior:** dialog — opens a config dialog on selection (evidence: `subaction-Integrations-Throwing-System-Throw-Item-dialog-uia.txt`; see `creation-flows-catalog.txt`)
**Description:** Allows for throwing a specific item
**Parameters:** App-verified from `subaction-Integrations-Throwing-System-Throw-Item-dialog-uia.txt`, `subaction-Integrations-Throwing-System-Throw-Item-dialog.png` (Add SubAction - Integrations > Throwing System > Throw Item, ~600×221): `Not Connected!` (text), `Delay` (text), `seconds` (text), `Amount` (text), `Test` (button), `Not Connected` (button); Ok/Cancel present.
**Variables:** Not documented in official docs

## Throw Items by Name

**Location:** `Integrations > Tits > Throw Items by Name`
**Add behavior:** dialog — opens a config dialog on selection (evidence: `subaction-Integrations-Throwing-System-Throw-Items-by-Name-dialog-uia.txt`; see `creation-flows-catalog.txt`)
**Description:** Allows for throwing multiple items by name
**Parameters:** App-verified from `subaction-Integrations-Throwing-System-Throw-Items-by-Name-dialog-uia.txt`, `subaction-Integrations-Throwing-System-Throw-Items-by-Name-dialog.png` (Add SubAction - Integrations > Throwing System > Throw Items by Name, ~600×273): `Item` (text), `Delay` (text), `seconds` (text), `Amount` (text), `Test` (button), `Not Connected` (button); Ok/Cancel present.
**Variables:** Not documented in official docs

## Throw Items

**Location:** `Integrations > Tits > Throw Items`
**Add behavior:** dialog — opens a config dialog on selection (evidence: `subaction-Integrations-Throwing-System-Throw-Items-dialog-uia.txt`; see `creation-flows-catalog.txt`)
**Description:** Allows for throwing a specific item
**Parameters:** App-verified from `subaction-Integrations-Throwing-System-Throw-Items-dialog-uia.txt`, `subaction-Integrations-Throwing-System-Throw-Items-dialog.png` (Add SubAction - Integrations > Throwing System > Throw Items, ~600×231): `Not Connected!` (text), `Delay` (text), `seconds` (text), `Amount` (text), `Test` (button), `Not Connected` (button); Ok/Cancel present.
**Variables:** Not documented in official docs
