## 1. Capture tooling (WSL-driven pipeline)

- [x] 1.1 Pre-flight: `powershell.exe` invocation from WSL proven — Streamer.bot window found via UIA (PID 10952, "Streamer.bot - Streamer.bot (v1.0.4)")
- [x] 1.2 Create capture scripts in `/mnt/d/Twitch/shots/`: `settings-click.ps1` (click section by name + pane dump), `settings-probe.ps1` (coord/click/right-click/wheel/key), `settings-uiact.ps1` (ExpandCollapsePattern combo expansion, expander invoke, deep dump), `settings-dump-all.ps1` (full-window dump with control types)
- [x] 1.3 Pilot complete on General Settings: click → dump, wheel-scroll states, both dropdowns expanded via UIA pattern, ESC close — output naming `settings-<section>-<state>-uia.txt` + `.png` established

## 2. Landing page verification

- [x] 2.1 Landing list fully scrolled to bottom (PageDown on content scrollbar x≈-645): exactly 8 items confirmed — General Settings, User Interface, Backups, Defaults, Groups, Variables, Labs, C# Compile Settings; the 8th item (C# Compile Settings) is fully visible when scrolled (button y=593..668 vs pane bottom 685) and NO 9th item exists (content pane ends at C# Compile Settings, then directly the ScrollBar); thumb near-max scroll proves the list bottom; evidence: `settings-landing-pd-check-uia.txt`
- [x] 2.2 Eight section names and in-app descriptions confirmed against the existing `Settings-uia.txt`

## 3. Panel captures — General Settings (done) + remaining seven sections

- [x] 3.1 General Settings — panel dump, scrolled states (`wheel1..6`), Audio Output Device dropdown (8 entries, "(Disconnected)" markers), Log Level dropdown (Information/Debug/Verbose), expander-chevron probes (decorative), Save-button and Log Folder browse identified; evidence: `settings-general*.png` + `-uia.txt`
- [x] 3.2 User Interface — panel dump (+ scrolled dump if content extends below fold); complete: 7 sections mapped (Window Options, Colors, Sub-Actions, Other, Navigation Panel, Enabled Sub-Actions, Enabled Triggers); Navigation Panel 6 expanders all expanded (Platforms: Twitch/YouTube/Trovo/Kick; Stream Apps: OBS Studio/Streamlabs Desktop/Meld Studio/PolyPop; Services: File-Folder Watcher/Timers/File Tails/Process Watcher/Credits/Quotes; MIDI: MIDI In/MIDI Out; Voice Control: Settings/Commands/Log; Servers/Clients: Websocket Server/HTTP Server/UDP Server/Custom Websocket Servers/Custom Websocket Clients); both checkbox trees **fully expanded to max depth** via `expand-trees` batch (4 passes: 42+135+9+0 → 189 expanders, dump 4749 lines): Enabled Sub-Actions 14 top-level categories with 2–3 nested levels (Core → 22 sub-categories with leaf sub-actions; Meld Studio → Effects/Layers/Tracks; OBS Studio → Filters/Groups/Replay/Scenes/Sources; Elgato → Stream Deck/Wave Link/Camera Hub → EpocCam/Prompter/Webcam; Twitch → Moderation → Chat Modes/Shield at depth 3), Enabled Triggers 11 top-level categories (Core → 15 sub-categories, Websocket → Client/Custom Server at depth 3; Integrations → 18 sub-categories; Twitch → Shared Chat with 18 leaves); each tree has its own internal scrollbar, content virtualized to document-y ≈16700; restart-banner texts captured; evidence: `ui-toggle-platforms/streamapps/services/midi/voice/servers-{png,uia}`, `ui-tree-{meld,obs,trigger-core}`, `ui-trees-expanded{2,3,4}-uia.txt` + `ui-trees-bottom{,-deep}`
- [x] 3.3 Backups — panel dump + all-windows dump; complete: 3 sections mapped (Backup Folder: "The folder to save backups to" + `[Edit]` field + unnamed browse `[Button]`; Backup Status: "Last backup: …" + `[Button] 'Backup Now'`; Streamer.bot Cloud Backups: Patreon perk text "Thank You tier or higher" + `[Button] 'Support on Patreon'` + `[Button] 'Streamer.bot Account'` with icon); button probes: **Backup Now** → executes backup immediately, NO confirmation dialog, timestamp updates (Aug 4 8:45 AM → Aug 5 8:38 AM) — live action, not a settings change; **browse** → opens standard Windows folder picker `Choose your backup folder` (640×360, IFileDialog/FOS_PICKFOLDERS: toolbar Organise/New folder/Views/Help, tree view, items view, address `D:\Twitch\Streamer.bot-x64-1.0.4\backup`, "Folder:" field, Select Folder/Cancel) — canceled without confirming; **Streamer.bot Account** → opens Chrome to "Sign in to Streamer.bot" (external browser, not logged in; left open, no interaction); evidence: `settings-backups{-uia,-deep-uia}.txt` + `settings-backups-uia.png`, `settings-backups-backupnow{-uia,.png}`, `settings-backups-browse-dialog-uia.txt` + `settings-backups-browse.png` (dialog), `settings-backups-browse-cancel{-uia,.png}`
- [x] 3.4 Defaults — panel dump + scrolled states; complete: 3 expandable sections (all expanded via new `expand-expanders` batch, 2 passes: 3+0 → 4 expander-toggles, incl. nested Sources): **Actions** (4 toggles: Random "Whether or not to pick a random top level sub-actions to only run", Concurrent "…run concurrently", Always Run "…will always run, regardless if the queue is paused", Exclude from Action Queue/History — all Off), **Commands** (Include Off; Ignore Bot Messages On; Ignore Internal Messages On; Persist User Counter Off; Persist Counter Off; Case Sensitive Off; Global Cooldown numeric stepper + "seconds"; User Cooldown numeric stepper + "seconds"; nested **Sources** expander "What sources are enabled by default" with 8 toggles: Twitch Message On, Twitch Whisper/Bot Whisper/Resubscription Message/Shared Chat/YouTube Message/Trovo Message/Kick Message Off), **Sub-Actions > Switch Case** (Case Sensitive "Whether or not to the switch case should be case sensitive", On); scroll technique: content scrollbar at x≈-646, PageUp/PageDown tracks clickable (wheel too slow ~48px/notch, HOME key doesn't reach ScrollViewer), panel ~1700px vs 560px viewport; evidence: `settings-defaults{-uia,.png}`, `settings-defaults-{actions,commands,sources}-open-uia.txt`, `settings-defaults-exp{1,2}`, `settings-defaults-pageup2`, `settings-defaults-pd1..6`, `settings-defaults-bottom-full{-uia,.png}`
- [x] 3.5 Groups — panel dump + full dump; complete: 3-column layout (Groups DataGrid with Name/Bots columns + Add Group section | Users list | Add Multiple Twitch Users to Group), all visible without scrolling; **Add Group**: Edit (name) + `[Button] 'Bots' [Toggle:Off]` (type toggle — probe: Off→On→Off, no popup, label unchanged) + `[Button] 'Add'` (empty name → nothing happens, no validation dialog); **row context menu** (right-click on a group row, 6 items): Delete / Clear / Search for Twitch User / Bots / Export to File / Import from File (no Edit item — editing via other paths); **Delete dialog**: `Delete group '<name>'?` (460×168, "Are you sure you want to delete the group '<name>'?", Yes/No); **Add flow** verified with temporary test group `zz-test` (user-approved, created + deleted): typing into name field shows `#ClearButton`, clicking Add inserts DataItem row (raw-id name, e.g. `_xIAJmWAhYOPk2W6E6WUANT1BCFB`) with the group name text; right-click on empty grid → no context menu (menu only on rows); evidence: `settings-groups{-uia,.png}`, `settings-groups-full-uia.txt`, `settings-groups-bots-toggle/restore`, `settings-groups-row-rclick` + `settings-groups-row-menu-uia.txt` (menu), `settings-groups-delete-click` + `settings-groups-delete-dialog-uia.txt` + `settings-groups-delete-dialog-pic.png` (dialog), `settings-groups-add-typed` (zz-test row), `settings-groups-after-delete` (clean state)
- [x] 3.6 Variables — panel dump + probes; complete: single section **Variable Masking** — `[Button] 'Mask Variable Containing' [Toggle:Off]` (feature toggle; probe: Off→On→Off, panel structure unchanged — toggles masking itself), `[List]` of masked words (user data: apiKey, url), `[Edit]` field + `[Button] 'Add'` + `[Button] 'Del'`, description text "Variables that contain these words will have their values masked with * in the following areas: 1. Global Variable Viewer 2. Action History Variable Inspecting"; **Add flow** (user-approved temp word, added + removed): clicking the Edit field pre-fills it with the currently selected list item's text, typing appends ('apiKey' + 'zz-testmask' → entry 'apiKeyzz-testmask'), Add inserts the field content as a new masked word and clears the field; `#ClearButton` clears the field; **Del flow (user-provided, verified)**: type the exact variable name into the field, THEN click Del — removes the matching entry, NO confirmation dialog; probes that do NOT delete: Del click with only a selected list item, keyboard Delete key, right-click on item (no context menu), double-click (nothing), Shift+F10/APPS key (no menu); no context menu anywhere on the list; evidence: `settings-variables{-uia,-full-uia}.txt`, `settings-variables-uia.png`, `settings-variables-{mask-toggle,mask-restore,mask-state}`, `settings-variables-{edit-focus,word-typed,added,item3-rclick,del-with-name,del-with-name-check,final-clean}`
- [x] 3.7 Labs — panel dump + probes; complete: single section **Pending Action Cache** — `[Button] 'Enable' [Toggle:Off]` (labs feature toggle, left untoggled: enabling requires restart and writes a DB on close — a state mutation) + 5-paragraph description (restart REQUIRED after enabling/disabling; on close writes outstanding pending actions to a database; on start auto-loads cached actions and pauses their queues, adds `wasCached` argument = true; "very experimental, and I need feedback"; caveat: serialization loses some types, List<T> → object[]); all content visible without scrolling, no panel scrollbar (text ends y≈431, pane bottom 685); evidence: `settings-labs2-uia.txt`, `settings-labs-click2`, `settings-labs-panel{-uia,.png}` + obs-1 verification (breadcrumb "Settings > Labs", Enable toggle Off, 5 paragraphs legible, no scrollbar)
- [x] 3.8 C# Compile Settings — panel dump + context-menu probe + Add Reference dialog; complete: single section **Common References** — `[List]` (522×150, empty on this install) + 2 description texts: "Any references added to the common references list above will be automatically added to every Execute C# code when it's compiled" and "Making Changes to this list will not invalidate any currently compiled Execute C# Code sub-actions"; all content fits viewport, no panel scrollbar; no Add/Del buttons in the panel — adding is via **right-click context menu on the list** with single item **"Add reference from file..."** (178×41); that item opens the standard Windows file picker **"Choose a library file"** (640×360, IFileDialog: toolbar Organise/New folder/Views/Preview pane/Help, tree view, items view with Name/Date modified/Type/Size columns, Vertical Scroll Bar) — canceled without confirming; evidence: `settings-csharp2-uia.txt`, `settings-csharp-click2`, `settings-csharp-list-rclick` + `settings-csharp-rclick-check-uia.txt` (menu), `settings-csharp-addref-click` + `settings-csharp-addref-dialog-uia.txt` (258 lines) + `settings-csharp-addref-dialog-pic.png` (dialog), `settings-csharp-dialog-cancel` + `settings-csharp-after-cancel` (clean state)

## 4. Visual verification

- [x] 4.1 Observer review of General Settings screenshots completed: toggle switches (Use System Default "Yes", Viewers Tab "Enabled", Action History "Enabled"), slider at 100%, dark theme, yellow restart-warning banners, "Name" text field showing "Streamer.bot" + Save button
- [x] 4.2 Observer review of remaining seven sections' screenshots as they are captured — User Interface trees done: `ui-tree-obs.png` verified via obs-1 (Servers/Clients nav section visible with 5 items, both trees' Core rows peeking at bottom edge, restart banners in UIA dump, tree-internal scrollbars confirmed — Sub-Actions content extends far below viewport due to 3 expanded categories, Triggers tree all collapsed); Backups done: `settings-backups-uia.png` + `settings-backups-browse.png` verified via obs-1 (panel: Backup Folder field shows `D:\Twitch\Streamer.bot-x64-1.0.4\backup`, Backup Now with cloud icon, Cloud Backups text with bold "Thank You"/"Patreon"/"Streamer.bot", heart icon on Patreon button, external-link icon on Account button; dialog: standard Win32 IFileDialog, address bar path matches UIA, "No items match your search." empty state, Select Folder/Cancel; timestamp difference between screenshots confirms the Backup Now action); Defaults done: `settings-defaults-pageup2.png` + `settings-defaults-pd2.png` verified via obs-2 (top: Actions 4 rows all Off + Commands Include Off + Switch Case header cut at bottom edge; mid: Ignore Bot/Internal Messages On, Persist toggles Off, Case Sensitive Off, Global/User Cooldown NumericUpDown steppers = 0 seconds, Sources expanded with Twitch Message On) + `settings-defaults-bottom.png` verified via obs-1 (bottom: Sources rows Whisper…Kick all Off, Sub-Actions plain-text divider, Switch Case expanded with Case Sensitive On; "Twitch Message" On row scrolled above viewport per UIA; note: dump-all captures no PNG — bottom screenshot re-taken with probe); Variables done: `settings-variables-item-textclick.png` + `settings-variables-del-clean.png` verified via obs-1 (list of 3 masked words incl. test entry, clicked item highlighted blue = selected, Add/Del buttons enabled, Edit field pre-fills with selected item's text, no popups/dialogs after Del, pink focus border on list, toggle Off — selection persists after Del click, only Edit text cleared; confirms Del does not act on mere list selection, matching the typed-name + Del flow); Labs done: `settings-labs-panel.png` verified via obs-1 (breadcrumb "Settings > Labs", single section Pending Action Cache with Enable toggle Off, 5 paragraphs legible, no scrollbar); C# Compile Settings done: `settings-csharp-click2.png` + `settings-csharp-addref-dialog-pic.png` verified via obs-1 (panel: breadcrumb "Settings > C# Compiler", empty Common References list + 2 description texts matching UIA, no buttons, no scrollbar; dialog: "Choose a library file" IFileDialog, address bar `C:\Windows\Microsoft.NET\Framework64\v4.0.30319`, 16 items = 12 folders + 4 DLLs, "File name" empty, type filter "Dynamic Link Library (*.dll)", Open/Cancel; dialog positioned top-left)

## 5. Docs coverage cross-check

- [x] 5.1 Docs coverage verified via @librarian (docs.streamer.bot + streamerbot-wiki GitHub repo): all 8 sections have **placeholder pages** under `docs.streamer.bot/en/Settings/*` ("Documentation Needed" — no content), so no section has a filled Settings page; real official documentation exists elsewhere for 4 sections — Backups (`docs.streamer.bot/guide/core/backup` Guide page), Groups (C# API `docs.streamer.bot/api/csharp/methods/core/users`: AddGroup/DeleteGroup/GetGroups/AddUserToGroup), Variables (C# API `docs.streamer.bot/api/csharp/methods/core/globals`: GetGlobalVar/SetGlobalVar/UnsetGlobalVar/GetGlobalVarValues + wiki `Variables/` folder), C# Compile Settings (`docs.streamer.bot/api/sub-actions/core/csharp/execute-csharp-code` + `docs.streamer.bot/api/csharp/guide/debugging`); **General Settings, User Interface, Defaults, Labs — app-evidence only** (no substantial docs); wiki source files: `streamerbot-wiki` repo `Settings/` folder (General.md, User-Interface.md, Groups.md, CSharp-Compiler.md exist; Backup.md at root; no Defaults.md/Labs.md); cross-links recorded for the reference page Evidence gaps / docs-coverage notes

## 6. Write the reference page (General Settings section first)

- [x] 6.1 Create `tools/automation/streamer-bot/settings.md` skeleton + **General Settings section** using the captured evidence: Settings Overview (landing table + scroll note, 8 sections confirmed), the full General Settings subsection (Instance/Audio/Performance/Logging with control types, toggle states, dropdown contents — 8 audio + 3 log entries, decorative-chevron note), disambiguation note cross-linking `sub-actions/settings.md`, Evidence gaps, docs-coverage table (from 5.1)
- [x] 6.2 Extend `settings.md` with the remaining seven sections (User Interface, Backups, Defaults, Groups, Variables, Labs, C# Compile Settings) — placeholders currently in file; all captures done (3.2–3.8)

## 7. Library integration

- [x] 7.1 Add the Settings reference to the UI Flow References list in `tools/automation/streamer-bot/overview.md`
- [x] 7.2 Run `node .opencode/scripts/rebuild-index.js` and `node .opencode/scripts/validate-links.js`; both must pass — `rebuild-index.js` regenerated INDEX.md (56 plugins found, exit 0); `validate-links.js` passed (112 links valid, exit 0). Pre-existing anomaly, unrelated to this change: stray 2-line stub `plugins/viewer-engagement/viewer-engagement/all-in-one-interactions/tangia.md` (doubled path, Jul 27) flagged `[ORPHAN]` — not touched (out of scope; destructive action requires user request).
